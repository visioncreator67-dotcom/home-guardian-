import React, { useState, useEffect } from 'react'; import { Button, Switch, Input, Select } from '../components/ui'; import { useVoice } from '../context/VoiceContext'; export default function Settings() { const { isVoiceActive, setIsVoiceActive, codeWord, setCodeWord, repetitions, setRepetitions, testVoice } = useVoice(); const [confirmCodeWord, setConfirmCodeWord] = useState(''); const [localCodeWord, setLocalCodeWord] = useState(''); const [localRepetitions, setLocalRepetitions] = useState(3); useEffect(() => { const savedCodeWord = localStorage.getItem('voiceCodeWord'); const savedRepetitions = localStorage.getItem('voiceRepetitions'); if (savedCodeWord) setLocalCodeWord(savedCodeWord); if (savedRepetitions) setLocalRepetitions(Number(savedRepetitions)); }, []); useEffect(() => { if (isVoiceActive && localCodeWord) { setCodeWord(localCodeWord); setRepetitions(localRepetitions); } }, [isVoiceActive, localCodeWord, localRepetitions]); const handleTestVoice = () => { if (!localCodeWord) { alert('Please set a code word first'); return; } testVoice(); }; const handleSaveSettings = () => { if (isVoiceActive && localCodeWord) { localStorage.setItem('voiceCodeWord', localCodeWord); localStorage.setItem('voiceRepetitions', localRepetitions.toString()); setCodeWord(localCodeWord); setRepetitions(localRepetitions); alert('Voice settings saved successfully!'); } else { alert('Please activate voice and set a code word first'); } }; return ( <div className="min-h-screen bg-gray-50 flex items-center justify-center p-4"> <div className="mt-16"> <h2 className="text-2xl font-bold text-center text-gray-800">Voice Command Settings</h2> <div className="space-y-6 mt-6"> <div> <Switch checked={isVoiceActive} onChange={setIsVoiceActive} className="w-48 h-16" /> <p className="text-center text-gray-600 mt-2"> {isVoiceActive ? 'Voice Activation: ON' : 'Voice Activation: OFF'} </p> </div> {isVoiceActive && ( <div> <label className="block text-sm font-medium text-gray-700 mb-2"> Secret Code Word </label> <Input placeholder="Enter your secret code word" value={localCodeWord} onChange={(e) => setLocalCodeWord(e.target.value)} className="mb-2 w-full" /> <Input placeholder="Confirm code word" value={confirmCodeWord} onChange={(e) => setConfirmCodeWord(e.target.value)} className="w-full" /> {localCodeWord && confirmCodeWord && localCodeWord !== confirmCodeWord && ( <p className="text-xs text-red-500 mt-1"> Code words do not match </p> )} </div> )} {isVoiceActive && ( <div> <label className="block text-sm font-medium text-gray-700 mb-2"> Number of Repetitions </label> <Select value={localRepetitions} onValueChange={(value) => setLocalRepetitions(Number(value))} className="w-full"> {[2, 3].map(value => ( <option key={value} value={value}> {value} time{value > 1 ? 's' : ''} </option> ))} </Select> </div> )} <Button variant="outline" onClick={handleTestVoice} className="w-full mt-4" disabled={!isVoiceActive || !localCodeWord}> Test Voice Detection </Button> {isVoiceActive && localCodeWord && ( <Button variant="solid" onClick={handleSaveSettings} className="w-full mt-4"> Save Settings </Button> )} {localCodeWord && ( <div className="mt-4 p-3 bg-blue-50 rounded"> <p className="text-sm text-blue-800"> Current code word: "<strong>{localCodeWord}</strong>" (say it <strong>{localRepetitions}</strong> times in a row to activate emergency) </p> </div> )} </div> </div> </div> ); }