import React, { useEffect } from 'react'; import { useNavigate } from 'react-router-dom'; import { useVoice } from '../context/VoiceContext'; export const VoiceListener: React.FC = () => { const navigate = useNavigate(); const { isVoiceActive } = useVoice(); useEffect(() => { const handleEmergency = () => { navigate('/emergency'); }; window.addEventListener('voice-emergency-triggered', handleEmergency); return () => window.removeEventListener('voice-emergency-triggered', handleEmergency); }, [navigate]); return null; };